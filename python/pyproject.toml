[tool.poetry]
name = "synapticrelay"
version = "1.0.0"
description = "SynapticRelay Pull-Based SDK"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
